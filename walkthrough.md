# Walkthrough: Dashboard Concept Refactoring & Interactive Controls

ตามกฎ **Dashboard Concept** (`.agents/rules/dashboard_concept.md`) และมาตรฐาน **Principal Designer** (`.agents/rules/principal_designer.md`) ระบบได้รับการ Refactor ปุ่มกดและตัวเลือกทั้งหมด ให้มีคุณสมบัติ:
1. **Space-Efficient (ประหยัดพื้นที่แนวตั้ง)**: ลดความสูงของแถบควบคุม ไม่กินพื้นที่หน้าจอโดยไม่จำเป็น เพื่อให้ข้อมูลกราฟและเซนเซอร์เด่นชัดที่สุด
2. **Data-Deferent (ไม่แย่งความสนใจจากกราฟ/ข้อมูลหลัก)**: แก้ไขปุ่มสีฟ้าสว่างจ้าเกินไป (Radioactive Cyan `#00d4ff`) ให้เป็นโทน Cobalt & Electric Cyan หรูหรา สบายตา ตามภาพตัวอย่างที่ 1 และ 2
3. **High-Frequency Interaction (สลับสถานะได้รวดเร็ว)**: ตอบสนองในคลิกเดียว พร้อมแอนิเมชันเลื่อนสลับสถานะแบบลื่นไหล

---

## 1. ผลลัพธ์การนำทั้ง 4 รูปแบบ (Dashboard Concept) ไปใช้งาน

### 1.1 Segmented Control / Sliding Pill Switcher (รูปแบบที่ 1)
- **คอมโพเนนต์**: สร้าง [`SegmentedControl.tsx`](file:///c:/Users/usEr/Desktop/Project/Project_FontEnd/src/components/ui/SegmentedControl.tsx) แบบ Reusable
- **ลักษณะ**: แคปซูลแบน (iOS / macOS style) มีไฮไลต์เลื่อนตามตำแหน่งที่เลือกอย่างนุ่มนวล พร้อมแสดง Badge จำนวน และไอคอน SVG
- **จุดที่นำไปใช้งาน**:
  - **หน้า Dashboard**: สลับมุมมองข้อมูลใน Telemetry Hub ระหว่าง `[ เซนเซอร์ทุกค่า | กราฟแนวโน้ม ]`
  - **หน้าประวัติเหตุการณ์ (DataHistoryPage)**: สลับช่วงเวลาด่วน `[ ทั้งหมด | 24 ชม. | 7 วัน | 30 วัน | กำหนดวันเอง ]` (คลิกเดียวกรองได้ทันทีโดยไม่ต้องกรอกวันที่) และสลับมุมมอง `[ ตารางข้อมูล | กราฟสถิติ ]`
  - **หน้ากราฟระดับน้ำ (ChartPage)**: สลับช่วงเวลา `[ รายชั่วโมง | รายวัน | รายสัปดาห์ ]`
  - **หน้าจัดการผู้ใช้ (UsersPage)**: สลับกลุ่มบทบาท `[ ทั้งหมด | ประชาชน | เจ้าหน้าที่ | ผู้ดูแลระบบ ]`
  - **หน้าจัดการสถานี (StationsPage)**: สลับมุมมอง `[ ตาราง | แผนที่ ]`

---

### 1.2 Metric / KPI Filter Tabs (การ์ดสรุปตัวเลขที่เป็นตัวกรองในตัว - รูปแบบที่ 2)
- **แก้ปัญหาตามภาพที่ 3 (Image 3)**: เดิมแถบตัวกรองและกล่อง KPI แยกแถวกันสิ้นเปลืองพื้นที่แนวตั้ง และการ์ด KPI ด้านล่างเป็นเพียงกล่องสถิตินิ่งๆ กดอะไรไม่ได้
- **การ Refactor ใน [DataHistoryPage.tsx](file:///c:/Users/usEr/Desktop/Project/Project_FontEnd/src/pages/DataHistoryPage.tsx)**:
  - แปลงการ์ดสรุปตัวเลขทั้ง 4 ใบให้เป็น **Interactive Filter Tabs**:
    1. **จำนวนครั้ง / ทั้งหมด (`16 รายการ`)**: คลิกเพื่อแสดงรายการทั้งหมดตามลำดับเวลาปกติ
    2. **ระดับน้ำต่ำสุด (`1.57 m`)**: คลิกแล้วการ์ดจะกลายเป็น Active (กรอบสีเขียวมรกต + เส้นไฮไลต์ใต้การ์ด) พร้อมเรียงลำดับตารางให้แสดงค่าระดับน้ำต่ำสุดก่อนทันที
    3. **ระดับน้ำสูงสุด (`1.57 m`)**: คลิกแล้วการ์ดจะกลายเป็น Active (กรอบสีส้มทอง + เส้นไฮไลต์ใต้การ์ด) พร้อมเรียงลำดับตารางให้แสดงระดับน้ำพีค/จุดเสี่ยงก่อนทันที
    4. **ระดับน้ำเฉลี่ย (`1.57 m`)**: คลิกแล้วการ์ดจะกลายเป็น Active (กรอบสีม่วงลาเวนเดอร์) พร้อมเทียบข้อมูลกับเกณฑ์เฉลี่ย
  - **ผลลัพธ์**: ลดความซ้ำซ้อน ไม่ต้องมีปุ่ม Sort/Filter แยกออกจากพื้นที่สรุปสถิติ

---

### 1.3 Floating Command Bar / Action Dock (รูปแบบที่ 3)
- **คอมโพเนนต์**: [`FloatingActionDock.tsx`](file:///c:/Users/usEr/Desktop/Project/Project_FontEnd/src/components/dashboard/FloatingActionDock.tsx)
- **ลักษณะ**: แถบลอยตัว (Floating Glass Dock) ที่ด้านล่างกึ่งกลางหน้าจอ (`backdrop-filter: blur(16px)`)
- **การทำงาน**:
  - สลับสถานีตรวจวัดได้ทันที
  - ปุ่มกด Refresh ข้อมูลสดพร้อมแอนิเมชันหมุนไอคอน
  - ปุ่มเลื่อนอัตโนมัติไปยังพิกัดบนแผนที่ GIS (`#map-section`)
  - ปุ่มทางลัดเปิดกราฟวิเคราะห์เชิงลึกแบบเต็มหน้าจอ (`/chart`)
- **จุดเด่น**: เข้าถึงคำสั่งสำคัญได้ตลอดเวลาไม่ว่าจะเลื่อนหน้าจอไปจุดใด และไม่กินพื้นที่คงที่ของ Dashboard

---

### 1.4 Compact Dropdown with Badges & Counts (รูปแบบที่ 4)
- **คอมโพเนนต์**: สร้าง [`CompactFilterDropdown.tsx`](file:///c:/Users/usEr/Desktop/Project/Project_FontEnd/src/components/ui/CompactFilterDropdown.tsx) แบบ Reusable
- **แก้ปัญหาตามภาพที่ 3 (Image 3)**: เดิมใช้แท็ก `<select>` แบบโบราณของเบราว์เซอร์ที่กินพื้นที่และไม่แสดงสถานะ
- **ลักษณะใหม่**:
  - เมื่อปิด: แสดงเป็นชิปสรุปกะทัดรัด พร้อมจุดสถานะสี (Status Dot: เขียว/ส้ม/แดง) และป้ายจำนวน
  - เมื่อเปิด: ป๊อปอัปกระจกหรูหรา (Obsidian Glass) แสดงรายชื่อสถานี, รหัสสถานี, ที่ตั้ง, ค่าระดับน้ำล่าสุด, และไอคอนเลือก
  - ปิดอัตโนมัติเมื่อคลิกนอกเมนู หรือกดปุ่ม `ESC`

---

## 2. แก้ปัญหาปุ่มสีฟ้าจ้าเกินไปตามภาพที่ 1 และภาพที่ 2 ([index.css](file:///c:/Users/usEr/Desktop/Project/Project_FontEnd/src/index.css))

- **ปัญหาเดิม**:
  - ภาพที่ 1: ปุ่มแคปซูลสีฟ้าสะท้อนแสงขนาดใหญ่ `"วิเคราะห์กราฟประวัติย้อนหลัง"` แย่งสายตาไปจากตัวเลขระดับน้ำ
  - ภาพที่ 2: ปุ่ม `+ เพิ่มผู้ใช้ใหม่` สีฟ้าจัดกระแทกสายตา และปุ่ม `รีเฟรช` ทรงทึบ
- **การปรับปรุง Design Tokens**:
  - **`.btn-primary`**: เปลี่ยนจากสีฟ้าเรืองแสงธรรมดา มาเป็น **Cobalt & Electric Cyan Gradient** (`linear-gradient(135deg, #2563eb 0%, #0284c7 100%)`) พร้อมขอบบาง `rgba(56, 189, 248, 0.35)` และเงาตกกระทบแบบนุ่มนวล ไม่แย่งความสนใจจากกราฟ
  - **`.btn-secondary`**: ปรับเป็นพื้นผิวกระจกฝ้า (`rgba(255, 255, 255, 0.04)`) พร้อมขอบหม่นละเอียด (`rgba(255, 255, 255, 0.12)`)
  - **ปุ่มเปิดกราฟใน Telemetry Hub**: ปรับเป็นปุ่มทรงกะทัดรัด `"กราฟเชิงลึก ↗"` แนบข้าง Segmented Switcher อย่างสมดุล

---

## 3. สรุปไฟล์ที่ได้รับการพัฒนาและ Refactor

| ไฟล์ที่แก้ไข / สร้างใหม่ | รายละเอียดการเปลี่ยนแปลง |
| :--- | :--- |
| [`.agents/rules/dashboard_concept.md`](file:///c:/Users/usEr/Desktop/Project/Project_FontEnd/.agents/rules/dashboard_concept.md) | บันทึกกฎและมาตรฐาน UI ทั้ง 4 รูปแบบของ Dashboard Concept เข้าสู่โปรเจกต์ |
| [`src/index.css`](file:///c:/Users/usEr/Desktop/Project/Project_FontEnd/src/index.css) | Refactor ปุ่ม `.btn-primary`, `.btn-secondary`, เพิ่มคลาส `.segmented-pill-track`, `.metric-filter-card` |
| [`src/components/ui/SegmentedControl.tsx`](file:///c:/Users/usEr/Desktop/Project/Project_FontEnd/src/components/ui/SegmentedControl.tsx) | คอมโพเนนต์สลับแคปซูลแบนพร้อม Sliding Pill Highlight (Pattern 1) |
| [`src/components/ui/CompactFilterDropdown.tsx`](file:///c:/Users/usEr/Desktop/Project/Project_FontEnd/src/components/ui/CompactFilterDropdown.tsx) | คอมโพเนนต์ดรอปดาวน์ขนาดกะทัดรัดพร้อม Status Dot & Badge Counts (Pattern 4) |
| [`src/components/ui/Icons.tsx`](file:///c:/Users/usEr/Desktop/Project/Project_FontEnd/src/components/ui/Icons.tsx) | เพิ่ม `ChevronDownIcon` แบบ SVG แท้ 100% (Zero-Emoji) |
| [`src/pages/DataHistoryPage.tsx`](file:///c:/Users/usEr/Desktop/Project/Project_FontEnd/src/pages/DataHistoryPage.tsx) | Refactor แถบตัวกรองและเปลี่ยนการ์ด KPI ให้เป็นตัวกรองในตัว (Pattern 2) + กราฟสถิติ |
| [`src/components/dashboard/StationTelemetryHub.tsx`](file:///c:/Users/usEr/Desktop/Project/Project_FontEnd/src/components/dashboard/StationTelemetryHub.tsx) | ใช้ `SegmentedControl` สลับโหมดเซนเซอร์/กราฟ และปรับปุ่มกราฟเชิงลึกให้กะทัดรัด |
| [`src/pages/UsersPage.tsx`](file:///c:/Users/usEr/Desktop/Project/Project_FontEnd/src/pages/UsersPage.tsx) | ใช้ `SegmentedControl` กรองบทบาท และปรับขนาดปุ่มรีเฟรช/เพิ่มผู้ใช้ใหม่ |
| [`src/pages/ChartPage.tsx`](file:///c:/Users/usEr/Desktop/Project/Project_FontEnd/src/pages/ChartPage.tsx) | ใช้ `CompactFilterDropdown` เลือกสถานี และ `SegmentedControl` เลือกช่วงเวลา |
| [`src/pages/StationsPage.tsx`](file:///c:/Users/usEr/Desktop/Project/Project_FontEnd/src/pages/StationsPage.tsx) | ใช้ `SegmentedControl` สลับมุมมองตาราง/แผนที่ และปรับปุ่มเพิ่มสถานี |

---

## 4. ผลการทดสอบและตรวจสอบคุณภาพโค้ด (Quality Checks)

- **TypeScript Type Check**: `npx tsc --noEmit` $\rightarrow$ **0 errors**
- **Oxlint**: `npm run lint` $\rightarrow$ **0 errors**
- **Vite Production Build**: `npm run build` $\rightarrow$ **ผ่านฉลุยใน 780ms**
---

## 5. การแก้ไขเพิ่มเติมตามฟีดแบ็กล่าสุด (รูปที่ 1 และ รูปที่ 2)

### 5.1 แก้ไขปัญหาข้อความซ้อนทับทะลุพื้นหลังในดรอปดาวน์ (รูปที่ 1)
- **สาเหตุ**: กล่อง Popover ของ `CompactFilterDropdown.tsx` เดิมใช้พื้นหลังกึ่งโปร่งใส (`rgba(15, 23, 42, 0.95)` + `backdrop-filter: blur(20px)`) เมื่อเปิดทับการ์ด KPI ด้านล่าง ทำให้ข้อความตัวเลขของการ์ดด้านหลังทะลุขึ้นมาซ้อนทับกับรายชื่อสถานีจนอ่านไม่ออก
- **การแก้ไข**:
  1. ปรับพื้นหลังของเมนู Popover ให้เป็น **สีทึบ 100% (Solid Obsidian Dark `#0D1526`)** เพื่อป้องกันการมองทะลุอย่างเด็ดขาด 100%
  2. กำหนด `zIndex: 9999` ให้เมนูลอยอยู่เหนือทุกองค์ประกอบในหน้าเว็บเสมอ
  3. กำหนด `position: 'relative'`, `zIndex: 50` ให้กับแถบควบคุมแม่ใน [`DataHistoryPage.tsx`](file:///c:/Users/usEr/Desktop/Project/Project_FontEnd/src/pages/DataHistoryPage.tsx)
  4. เพิ่มเงาตกกระทบแบบนุ่มลึก (`boxShadow: 0 24px 60px rgba(0, 0, 0, 0.95)`) เพื่อแยกชั้นมิติการมองเห็นอย่างชัดเจน

---

### 5.2 ปรับโฉมตัวเลือกสถานีตรวจวัดให้เลือกง่าย ชัดเจน ไม่เบียดขวา (รูปที่ 2)
- **ปัญหาเดิมในรูปที่ 2**: ปุ่มตัวเลือกสถานีเป็นแคปซูลขนาดเล็กที่ถูกเบียดไปอยู่ชิดขวาสุดของแถบแนวนอนอันยาวเหยียด ผู้ใช้ต้องกวาดสายตาหาไกล ปุ่มมีขนาดเล็ก คลิกยาก และตัดทอนชื่อสถานี
- **การออกแบบใหม่ใน [`StationSegmentedControl.tsx`](file:///c:/Users/usEr/Desktop/Project/Project_FontEnd/src/components/dashboard/StationSegmentedControl.tsx)**:
  - เปลี่ยนจากการเป็นแถบยาวแคบๆ มาเป็น **การ์ดเลือกสถานีตรวจวัดแบบเต็มความกว้าง (Station Selection Cards Grid)**:
    - **ขนาดพื้นที่คลิกใหญ่พิเศษ (Fitts's Law)**: คลิกที่จุดใดก็ได้บนการ์ดขนาดใหญ่ (>300px) เพื่อสลับสถานีทันที ไม่ต้องเล็งปุ่มเล็กๆ อีกต่อไป
    - **หัวการ์ด**: แสดงรหัสสถานีเด่นชัด (`ST-001`, `ST-002`) พร้อมไฟสถานะกะพริบสด (เขียวมรกต/ส้ม/แดง) และป้ายบอกสถานะการเลือก `[✓ กำลังแสดงข้อมูลนี้]`
    - **ชื่อสถานี & พิกัด**: แสดงชื่อสถานีขนาดใหญ่ 17px ชัดเจน (`มหาวิทยาลัยราชมงคลธัญบุรี`, `สถานีคลองรังสิต`) พร้อมที่ตั้งตำบล/อำเภอ
    - **ระดับน้ำสดขนาดใหญ่**: แสดงตัวเลขระดับน้ำจริง 24px (`1.55 ม.`, `0.00 ม.`) พร้อมป้ายสถานะความปลอดภัย
    - **Active State ที่ทรงพลัง**: เมื่อสถานีใดถูกเลือก การ์ดนั้นจะสว่างขึ้นด้วยกรอบสี Electric Cyan (`2px solid var(--cyan-glow)`), เงาเรืองแสง Cyan Glow, เส้นแถบไล่เฉดสีด้านบนการ์ด, และพื้นหลังที่ยกตัวสูงขึ้นอย่างโดดเด่น

---

## 6. ปรับโฉมหน้ากราฟระดับน้ำ (ChartPage Redesign: The Hero Graph)

ตามความต้องการของคุณที่ระบุว่า **"หลักๆ ของหน้านี้คือกราฟ"**, **"ตัดปุ่ม Export CSV 2 อันออก"**, และ **"เปลี่ยนฝั่งขวาให้เป็นตัวเลือกสถานีพร้อมระดับน้ำ แบต และค่าที่วัดได้เล็กๆ"**:

### 6.1 ยกระดับกราฟให้เป็นจุดโฟกัสหลัก (The Hero Graph)
- **ขยายขนาดกราฟเต็มพื้นที่**: ขยายความสูงกราฟเป็น **480px** บนการ์ดหลักฝั่งซ้าย (`minmax(0, 1fr)`) กว้างขวาง คมชัด
- **ยุบแถบควบคุมด้านบน**: ย้ายปุ่มสลับช่วงเวลา `[ 24 ชั่วโมง | 7 วัน | 30 วัน ]` (Segmented Control) มาไว้ที่มุมขวาบนของการ์ดกราฟโดยตรง ประหยัดพื้นที่แนวตั้งได้ทันทีกว่า 80px ทำให้เปิดหน้ามาเจอกราฟทันทีโดยไม่ต้องเลื่อนจอ
- **แสดงเกณฑ์และสเกลชัดเจน**:
  - เส้นประสีส้มทอง: เกณฑ์เฝ้าระวัง
  - เส้นประสีแดงเข้ม: เกณฑ์วิกฤต
  - เส้นกราฟระดับน้ำจริง: ไล่เฉดสีสีม่วงนีออนถึง Electric Cyan พร้อมพื้นที่ระบายสีแบบเงาเรืองแสงใต้กราฟ
  - แถบสรุปเกณฑ์สันตลิ่งสูงสุดและเกณฑ์ปกติใต้กราฟ

### 6.2 ตัดส่วนที่ไม่จำเป็นออกตามที่สั่ง
- นำปุ่ม `Export CSV` ในแถบควบคุมด้านบนออก
- นำปุ่มใหญ่ `ดาวน์โหลด CSV` ที่ด้านล่างขวาออก
- นำการ์ดสถิติตายตัว 4 ใบฝั่งขวา (ตำแหน่ง GPS, ระดับอ้างอิง, สภาพแวดล้อม, อุปกรณ์) ออกทั้งหมด

### 6.3 ปรับแผงฝั่งขวาเป็น "ตัวเลือกสถานีพร้อมข้อมูลเซนเซอร์ย่อส่วน" (Station Selector Mini-Cards)
- จัดวางสถานีทั้งหมดในระบบ (เช่น `ST-001`, `ST-002`) เป็นการ์ดปุ่มกดที่คลิกสลับได้ทันที:
  1. **สถานีที่กำลังเลือก (Active)**: มีกรอบเรืองแสงสี **Electric Cyan 2px**, ป้าย `[✓ กำลังดูกราฟ]`, เส้นไฮไลต์ด้านบน, และยกตัวเด่นชัด
  2. **ตัวเลขระดับน้ำจริง**: แสดงขนาดใหญ่ 20px พร้อมหน่วย เมตร และป้ายสถานะความปลอดภัย (`ปกติ` / `เฝ้าระวัง`)
  3. **ชิปเซนเซอร์ย่อส่วน (Mini-Metrics)**:
     - **แบตเตอรี่**: ไอคอนแบตเตอรี่ + เปอร์เซ็นต์ (เช่น `100%`) แยกสีตามระดับพลังงาน
     - **อุณหภูมิ**: ไอคอนปรอท + ค่าอุณหภูมิ (เช่น `27.8°`)
     - **ความชื้น**: ไอคอนหยดน้ำ + ค่าความชื้น (เช่น `74%`)
- **การใช้งาน**: คลิกที่การ์ดสถานีใดฝั่งขวา กราฟฝั่งซ้ายจะโหลดและอัปเดตประวัติระดับน้ำของสถานีนั้นทันทีแบบ Seamless!

### 6.4 ปรับแต่งหัวข้อให้เรียบหรู คลีน (Clean Header)
- นำไอคอนกล่องสี่เหลี่ยมสีฟ้าเรืองแสง (`LineChartIcon`) ด้านหน้าหัวข้อออกตามข้อเสนอแนะ
- คืนค่าหัวข้อมาตรฐาน: `กราฟระดับน้ำ` และคำอธิบายย่อ `ติดตามการเปลี่ยนแปลงระดับน้ำตามช่วงเวลา` เรียบง่าย สบายตา ไม่แย่งสายตาจากกราฟหลัก

---

## 7. ปรับปรุงหน้า Dashboard ตามข้อเสนอแนะล่าสุด

### 7.1 แบ่งพื้นที่ "แผนที่ GIS" (ซ้ายใหญ่) และ "ประวัติการตรวจวัดล่าสุด" (ขวา)
- **คอมโพเนนต์ใหม่**: [`StationRecentReadingsCard.tsx`](file:///c:/Users/usEr/Desktop/Project/Project_FontEnd/src/components/dashboard/StationRecentReadingsCard.tsx)
- **การจัด Layout แบบ 2 คอลัมน์**:
  - ฝั่งซ้าย: **แผนที่ภูมิศาสตร์จุดตรวจวัด (GIS Water Map)** ปรับสัดส่วนให้กว้างกว่า (`minmax(0, 1.25fr)` หรือประมาณ 56%)
  - ฝั่งขวา: **ประวัติการตรวจวัดล่าสุด (5 รายการล่าสุด)** (`minmax(0, 1fr)` หรือประมาณ 44%)
  - ความสูง 480px เท่ากันทั้งสองฝั่งอย่างสมดุล (Bento Card Grid) พร้อม Responsive Stacking บนหน้าจอแท็บเล็ต/มือถือ (`@media (max-width: 1024px)`)
- **ลดความละเอียดที่ไม่จำเป็น (Citizen-Friendly)**:
  - ตัดคอลัมน์ฮาร์ดแวร์เชิงลึกที่รบกวนสายตาออก ได้แก่: ค่าความแรงสัญญาณ (RSSI), มุมเอียงเสา (Tilt X/Y), และความชื้นสัมพัทธ์ (%RH)
  - คงไว้เฉพาะข้อมูลที่จำเป็นและเข้าใจง่าย 5 คอลัมน์:
    1. **เวลาบันทึก**: รูปแบบ `HH:mm:ss`
    2. **ระดับน้ำ**: ตัวเลขเด่นสี Electric Cyan (เช่น `1.540 ม.`)
    3. **สถานะความปลอดภัย**: Badge สี `● ปกติ` (เขียว) / `● เฝ้าระวัง` (ส้ม) / `● วิกฤต` (แดง)
    4. **แบตเตอรี่**: ไอคอนสถานะพลังงาน + เปอร์เซ็นต์ (เช่น `100%`)
    5. **อุณหภูมิ**: ไอคอนปรอท + องศาเซลเซียส (เช่น `27.3°C`)

### 7.2 แก้ปัญหากราฟแนวโน้มระดับน้ำย้อนหลัง (Water Level Trend) ถี่และซูมระดับมิลลิเมตร
- **ปัญหาเดิม**: แกน Y ใช้ `domain={['auto', 'auto']}` ทำให้เมื่อระดับน้ำผันผวนเพียง 3 เซนติเมตร (1.540 - 1.573 ม.) กราฟกลับซูมขยายเต็มหน้าจอ และสร้างเส้นสเกลระดับมิลลิเมตร (`539m`, `548m`, `557m`, `566m`, `575m`) ดูเหมือนน้ำลดฮวบและถี่จนตาลาย
- **การปรับปรุงใน [`StationTelemetryHub.tsx`](file:///c:/Users/usEr/Desktop/Project/Project_FontEnd/src/components/dashboard/StationTelemetryHub.tsx)**:
  - ตั้งค่าสเกลแกน Y ให้ครอบคลุมช่วงระดับน้ำจริงของสถานี: `domain={[0, Math.max(maxLevel, 7)]}`
### 7.3 ปรับสีข้อความความชื้น และเปิดการ Interact ให้การ์ดเกตเวย์
- **ปรับสีข้อความสถานะความชื้นสัมพัทธ์**:
  - เปลี่ยนสีข้อความ `"ความชื้นในบรรยากาศรอบสถานี (เกณฑ์ปกติ)"` จากสีเทา `var(--text-secondary)` ให้เป็น **สีเขียวมรกต `#10B981` (Font-weight: 600)** เหมือนกับการ์ดอื่นๆ ในกลุ่มอย่างสม่ำเสมอ
- **เปิดการ Interact ให้กับการ์ดเกตเวย์เชื่อมต่อ (Gateway Card)**:
  - เพิ่ม `role="button"`, `tabIndex={0}`, และ `onClick={() => setActiveMetric('gateway')}` พร้อมขยาย `type ActiveMetricKey` ให้รองรับ `'gateway'`
  - เมื่อคลิกที่การ์ดเกตเวย์ จะมีกรอบเรืองแสงสีเขียวมรกต (`1.5px solid #22C55E`), พื้นหลังยกตัวสูงขึ้น (`rgba(34, 197, 94, 0.08)`), และเคอร์เซอร์มือ (Pointer) ตอบสนองการกดอย่างสมบูรณ์แบบเหมือนกับการ์ดเซนเซอร์ทุกใบ

### 7.4 แก้ไขระยะห่างระหว่างการ์ดระดับน้ำสดกับการ์ดเซนเซอร์ด้านล่าง
- **ปัญหาเดิม**: การ์ด `"ระดับน้ำที่วัดได้จริง (Live Water Level)"` มีค่า `marginBottom: 0` ทำให้ขอบล่างของการ์ดชนติดกับขอบบนของการ์ดเซนเซอร์ทั้ง 8 ใบด้านล่างจนไม่มีช่องว่าง
- **การแก้ไข**: ปรับเพิ่ม `marginBottom: '1.25rem'` (20px) ให้เกิดระยะห่าง (Spacing & Visual Breathing Room) สบายตา สวยงามตามระบบ 8px Grid ของ Principal Design Standards

---

## 8. ระบบดูกราฟระดับน้ำย้อนหลัง (รายชั่วโมง/รายวัน/รายสัปดาห์) และส่งออกไฟล์ CSV

### 8.1 ยกระดับตัวเลือกสลับช่วงเวลากราฟใน [ChartPage.tsx](file:///c:/Users/usEr/Desktop/Project/Project_FontEnd/src/pages/ChartPage.tsx)
- ปรับขนาดปุ่ม Segmented Control เป็น **`size="md"`** พร้อมเสริมไอคอน SVG ประจำช่วงเวลาเพื่อให้มองเห็นชัดเจนและน่ากดเลือก:
  - **รายชั่วโมง** (`hourly`): ไอคอน `<ClockIcon />` ดึงข้อมูลระดับน้ำย้อนหลัง 24 ชั่วโมง
  - **รายวัน** (`daily`): ไอคอน `<ActivityIcon />` ดึงข้อมูลระดับน้ำย้อนหลัง 7 วัน
  - **รายสัปดาห์** (`weekly`): ไอคอน `<LineChartIcon />` ดึงข้อมูลระดับน้ำย้อนหลัง 30 วัน/4 สัปดาห์
- ใส่กรอบฐานกระจกฝ้าทรงแคปซูล (`rgba(8, 14, 26, 0.75)`) พร้อมเงาลึกแบบ Inset ให้แถบเลื่อน (Sliding Pill) ลอยเด่นชัดเจน

### 8.2 ปรับปุ่มส่งออก CSV ให้เข้าชุดกับปุ่มอื่นในระบบ (Clean & Consistent Design)
- **โทนสีและมิติ (Design Token Consistency)**:
  - ใช้คลาสมาตรฐานระบบ **`.btn .btn-secondary .btn-sm`** (พื้นหลังกระจกฝ้าหม่น `rgba(255, 255, 255, 0.04)`, ขอบบางละเอียด `1px solid rgba(255, 255, 255, 0.12)`, สีข้อความ `var(--text-primary)`, ความสูง 34px) เช่นเดียวกับปุ่มควบคุมอื่นในระบบ
- **Micro-interaction & Hover Feedback (ไม่มีแสงเรืองแสง)**:
  - ตัดเงาเรืองแสงสีฟ้า Cyan Glow (`boxShadow: none`) ออกทั้งหมด 100% ตามความต้องการ
  - เมื่อชี้เมาส์ (Hover) จะสว่างขึ้นอย่างนุ่มนวลตามมาตรฐาน `.btn-secondary:hover` (พื้นหลัง `0.08`, ขอบขาวบาง `0.22`, ข้อความ `#ffffff`) โดยไม่มีแสงวาบหรือเงาลอยรบกวนสายตา
  - มีข้อความและไอคอนเรียบง่าย สะอาดตา: `<DownloadIcon size={14} /> ส่งออก CSV` พร้อมแสดงสถานะ `✓ ดาวน์โหลดสำเร็จ` (สีเขียวเรียบง่าย ไม่เปลี่ยนสีพื้นหลังปุ่ม) เมื่อคลิกเสร็จสิ้น



